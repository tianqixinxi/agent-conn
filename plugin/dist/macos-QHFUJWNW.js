import { createRequire as __agentCommCr } from "node:module"; const require = __agentCommCr(import.meta.url);
import{AgentCommError,parseInviteLink}from"./chunk-PZB5KHL3.js";import{spawnSync}from"node:child_process";import{randomBytes}from"node:crypto";import{appendFileSync,chmodSync,existsSync,mkdirSync,rmSync,writeFileSync}from"node:fs";import{homedir}from"node:os";import{dirname,join}from"node:path";import{fileURLToPath}from"node:url";var APP_NAME="AgentComm Launcher.app";var PLIST_BUDDY="/usr/libexec/PlistBuddy";var LS_REGISTER="/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister";function shellQuote(value){return`'${value.replaceAll("'",`'"'"'`)}'`}function appleScriptString(value){return`"${value.replaceAll("\\","\\\\").replaceAll('"','\\"')}"`}function runChecked(command,args){const result=spawnSync(command,args,{encoding:"utf8"});if(result.error||result.status!==0){const detail=result.error?.message??result.stderr.trim()??`exit ${result.status}`;throw new AgentCommError("NOT_IMPLEMENTED",`${command} failed: ${detail}`)}return result.stdout.trim()}function findPluginRoot(start=dirname(fileURLToPath(import.meta.url))){let current=start;for(;;){if(existsSync(join(current,".claude-plugin","plugin.json")))return current;const parent=dirname(current);if(parent===current)break;current=parent}throw new AgentCommError("NOT_IMPLEMENTED","\u627E\u4E0D\u5230 agent-comm plugin root")}function parseLauncherUrl(rawUrl){let launcher;try{launcher=new URL(rawUrl)}catch{throw new AgentCommError("INVITE_INVALID","invalid agentcomm launcher URL")}if(launcher.protocol!=="agentcomm:"||launcher.hostname!=="open"){throw new AgentCommError("INVITE_INVALID","launcher URL must be agentcomm://open")}const invite=launcher.searchParams.get("invite");if(!invite)throw new AgentCommError("INVITE_INVALID","launcher URL missing invite");const parsed=parseInviteLink(invite);if(parsed.kind!=="relay"){throw new AgentCommError("INVITE_INVALID","browser launcher requires an HTTP relay invitation")}return{invite,relayUrl:parsed.relayUrl}}function buildClaudeLaunchCommand(input){const profile=input.profile&&input.profile!=="auto"?input.profile:void 0;const prompt=["This session was opened from an AgentComm invitation.","Use the AgentComm integration to connect with the invitation below.","The integration will require one host-enforced yes/no permission before establishing this new trust relationship.","After connecting, process safe channel work automatically and surface only permission or governance approvals.","",input.invite].join("\n");const env=["/usr/bin/env",`CLAUDE_PLUGIN_ROOT=${shellQuote(input.pluginRoot)}`,...profile?[`AGENT_COMM_CHANNEL_PROFILE=${shellQuote(profile)}`,`AGENT_COMM_CHANNEL_ALIAS=${shellQuote(profile)}`]:[],`AGENT_COMM_RELAY_URL=${shellQuote(input.relayUrl)}`];const launch=["cd",shellQuote(input.pluginRoot),"&&",...env,shellQuote(input.claudeBin),shellQuote(prompt),"--plugin-dir",shellQuote(input.pluginRoot),"--dangerously-load-development-channels","server:agent-comm"].join(" ");return`printf '\\nStarting AgentComm + Claude Code\u2026\\n\\n'; ${launch}`}function buildLauncherAppleScript(input){const path=[dirname(input.nodeBin),dirname(input.claudeBin),"/usr/bin","/bin"].join(":");const bootstrap=["/usr/bin/env",`PATH=${shellQuote(path)}`,`AGENT_COMM_CLAUDE_BIN=${shellQuote(input.claudeBin)}`,`AGENT_COMM_LAUNCH_PROFILE=${shellQuote(input.profile??"auto")}`,shellQuote(input.acBin),"handle-url",""].join(" ");const redirect=` >> ${shellQuote(input.logPath??join(homedir(),".agent-comm","launcher.log"))} 2>&1 &`;return`on open location theURL
  do shell script ${appleScriptString(bootstrap)} & quoted form of theURL & ${appleScriptString(redirect)}
end open location

on run
  return
end run`}function buildTerminalLauncherScript(command,scriptPath){return`#!/bin/zsh
# \u5220\u9664\u5305\u542B\u4E00\u6B21\u6027\u9080\u8BF7\u7684\u4E34\u65F6\u811A\u672C\uFF1B\u5F53\u524D shell \u5DF2\u7ECF\u628A\u5185\u5BB9\u8BFB\u5165\u3002
/bin/rm -f -- ${shellQuote(scriptPath)}
${command}
code=$?
if [ "$code" -ne 0 ]; then
  printf '\\nAgentComm launcher failed (exit %s).\\n' "$code"
  printf 'Details: ~/.agent-comm/launcher.log\\n'
  read -r '?Press Enter to close\u2026 '
fi
exit "$code"
`}function setPlistValue(plist,command){runChecked(PLIST_BUDDY,["-c",command,plist])}function installMacLauncher(opts={}){if(process.platform!=="darwin"){throw new AgentCommError("NOT_IMPLEMENTED","agentcomm:// launcher \u76EE\u524D\u4EC5\u652F\u6301 macOS")}const pluginRoot=findPluginRoot();const acBin=join(pluginRoot,"bin","ac");const builtMain=join(pluginRoot,"packages","agent-comm","dist","main.js");if(!existsSync(acBin)||!existsSync(builtMain)){throw new AgentCommError("NOT_IMPLEMENTED","\u8BF7\u5148\u5728 agent-comm \u4ED3\u5E93\u8FD0\u884C pnpm build:cli")}const claudeBin=runChecked("/usr/bin/which",["claude"]);if(!claudeBin)throw new AgentCommError("NOT_IMPLEMENTED","\u672A\u627E\u5230 Claude Code CLI");const appPath=opts.appPath??join(homedir(),"Applications",APP_NAME);const logPath=join(homedir(),".agent-comm","launcher.log");mkdirSync(dirname(appPath),{recursive:true});mkdirSync(dirname(logPath),{recursive:true,mode:448});rmSync(appPath,{recursive:true,force:true});const script=buildLauncherAppleScript({acBin,claudeBin,nodeBin:process.execPath,profile:opts.profile,logPath});runChecked("/usr/bin/osacompile",["-o",appPath,"-e",script]);const plist=join(appPath,"Contents","Info.plist");spawnSync(PLIST_BUDDY,["-c","Delete :CFBundleURLTypes",plist]);setPlistValue(plist,"Add :CFBundleURLTypes array");setPlistValue(plist,"Add :CFBundleURLTypes:0 dict");setPlistValue(plist,"Add :CFBundleURLTypes:0:CFBundleURLName string io.agentcomm.launcher");setPlistValue(plist,"Add :CFBundleURLTypes:0:CFBundleURLSchemes array");setPlistValue(plist,"Add :CFBundleURLTypes:0:CFBundleURLSchemes:0 string agentcomm");const setUiElement=spawnSync(PLIST_BUDDY,["-c","Set :LSUIElement true",plist]);if(setUiElement.status!==0)setPlistValue(plist,"Add :LSUIElement bool true");if(existsSync(LS_REGISTER))runChecked(LS_REGISTER,["-f",appPath]);return appPath}function handleLauncherUrl(rawUrl){if(process.platform!=="darwin"){throw new AgentCommError("NOT_IMPLEMENTED","agentcomm:// launcher \u76EE\u524D\u4EC5\u652F\u6301 macOS")}const target=parseLauncherUrl(rawUrl);const logPath=join(homedir(),".agent-comm","launcher.log");appendFileSync(logPath,`${new Date().toISOString()} opening Claude for ${target.relayUrl}
`,{mode:384});const claudeBin=process.env.AGENT_COMM_CLAUDE_BIN;if(!claudeBin)throw new AgentCommError("NOT_IMPLEMENTED","launcher \u672A\u8BB0\u5F55 Claude Code \u8DEF\u5F84\uFF0C\u8BF7\u91CD\u65B0\u5B89\u88C5");const command=buildClaudeLaunchCommand({...target,claudeBin,pluginRoot:findPluginRoot(),profile:process.env.AGENT_COMM_LAUNCH_PROFILE});const launchDir=join(homedir(),".agent-comm","launchers");mkdirSync(launchDir,{recursive:true,mode:448});const scriptPath=join(launchDir,`launch-${Date.now()}-${randomBytes(4).toString("hex")}.command`);writeFileSync(scriptPath,buildTerminalLauncherScript(command,scriptPath),{mode:448});chmodSync(scriptPath,448);try{runChecked("/usr/bin/open",["-a","Terminal",scriptPath]);appendFileSync(logPath,`${new Date().toISOString()} Terminal command file opened
`)}catch(err){rmSync(scriptPath,{force:true});appendFileSync(logPath,`${new Date().toISOString()} launch failed: ${String(err)}
`);throw err}}export{buildClaudeLaunchCommand,buildLauncherAppleScript,buildTerminalLauncherScript,findPluginRoot,handleLauncherUrl,installMacLauncher,parseLauncherUrl};
